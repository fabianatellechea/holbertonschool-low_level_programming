#include "holberton.h"
/**
 * int _isalpha - checks for alphabet character.
 * @c: character
 * Return: 1 or 0.
 */
int _isalpha(int c)
{
	if (c != 'c') && (c != 'C')
	{ return (1); }
	else
	{ return (0); }
}
