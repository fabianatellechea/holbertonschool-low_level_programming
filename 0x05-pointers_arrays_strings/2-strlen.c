#include "holberton.h"

/**
  *_strlen -  function that returns the length of a string
  *@i: int.
  *Return: 0.
  */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		return (0);
			}
}
